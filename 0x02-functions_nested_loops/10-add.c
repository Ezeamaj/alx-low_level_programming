#include "main.h"

/**
 * add - adds two ints and returns the result
 * @a: integers to be added to b
 * @b: integers to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
